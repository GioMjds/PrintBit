<UserControl
    x:Class="PrintBit.Presentation.Views.PrintConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <materialDesign:Card
            Grid.Column="0"
            Margin="8"
            Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border
                    Grid.Row="1"
                    Margin="0,12,0,0"
                    Padding="12"
                    BorderBrush="#FFBBBBBB"
                    BorderThickness="1"
                    CornerRadius="8">
                    <wv2:WebView2 x:Name="DocumentPreviewBrowser" />
                </Border>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card
            Grid.Column="1"
            Margin="8"
            Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Margin="0,0,0,12"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="Print Settings" />

                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Copies:" />
                    <Button
                        Margin="8,0,8,0"
                        Command="{Binding DecreaseCopiesCommand}"
                        Content="-" />
                    <TextBlock
                        MinWidth="30"
                        VerticalAlignment="Center"
                        Text="{Binding Copies}" />
                    <Button Command="{Binding IncreaseCopiesCommand}" Content="+" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,12,0,0">
                    <TextBlock Margin="0,0,0,4" Text="Color Mode" />
                    <ComboBox ItemsSource="{Binding ColorModes}" SelectedItem="{Binding SelectedColorMode, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Grid.Row="3" Margin="0,12,0,0">
                    <TextBlock Margin="0,0,0,4" Text="Pages" />
                    <ComboBox ItemsSource="{Binding PageSelectionModes}" SelectedItem="{Binding SelectedPageSelectionMode, Mode=TwoWay}" />
                    <StackPanel Margin="0,12,0,0" Visibility="{Binding IsPageRangeSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Margin="0,0,0,4" Text="Page Range (e.g. 1-3)" />
                        <TextBox Text="{Binding PageRange, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel
                    Grid.Row="4"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Margin="0,0,8,0"
                        Padding="10,6"
                        Command="{Binding GoBackToPrintCommand}"
                        Content="Back" />
                    <Button
                        Padding="10,6"
                        Command="{Binding GoToConfirmPrintCommand}"
                        Content="Continue to Confirm" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
